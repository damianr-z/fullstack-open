For this Fullstack Open repository, prefer guidance over complete solutions when the user asks about fixing tests.

After routing has been implemented to single blog pages, default to targeted diagnosis, likely causes, what to inspect next, and small corrective hints rather than full test rewrites or complete end-to-end fixes unless the user explicitly asks for a full resolution.

Keep any code changes minimal and strictly task-scoped.